apiVersion: v1
kind: Service
metadata:
  name: template-api
  labels:
    app: template-api
spec:
  type: ClusterIP
  selector:
    app: template-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
